## 1. Helper script + tests (TDD, sequential — same file)

- [x] 1.1 Write the fixture-dir test harness at `plugins/issue-driven-dev/scripts/tests/idd-worktree/test.sh` (mirroring the existing `scripts/tests/` precedent), with fixtures that each spin up a throwaway git repo and assert: `create` produces `.claude/worktrees/idd-<N>/` on branch `idd/<N>-<slug>`; `create` prints exactly the worktree path on stdout; `.claude/worktrees/` is appended to `.gitignore` once (idempotent on re-run); re-`create` is a no-op exit 0; branch-on-other-worktree → exit 4; `cleanup` removes a clean worktree but keeps the branch; dirty `cleanup` without `--force` → exit 5 and worktree intact; dirty `cleanup --force` removes it; missing `cleanup` → exit 0; `list` prints one line per IDD worktree. Run it now to confirm it FAILS (helper absent) — RED.
- [x] 1.2 Implement the `create` subcommand in `plugins/issue-driven-dev/scripts/idd-worktree.sh`: resolve repo root (`git rev-parse --show-toplevel` or `--repo-root`), compute `.claude/worktrees/idd-<N>/` and branch `idd/<N>-<slug>` (slug from `--slug`, else gh issue title slugified ≤40 chars, else bare `idd/<N>`), idempotent marker-guarded `.gitignore` append of `.claude/worktrees/`, `git worktree add -b` the branch from the default branch, print the absolute path to stdout, idempotent re-create, and exit codes 0/1/2/3/4 per the design Implementation Contract.
- [x] 1.3 Implement the `cleanup` subcommand in `plugins/issue-driven-dev/scripts/idd-worktree.sh`: `git worktree remove` the path, exit 0 no-op when absent, refuse exit 5 on uncommitted changes without `--force`, honor `--force`, and leave the feature branch intact.
- [x] 1.4 Implement the `list` subcommand in `plugins/issue-driven-dev/scripts/idd-worktree.sh`: enumerate worktrees under `.claude/worktrees/idd-*` and print `<N>  <branch>  <path>` one per line; empty output + exit 0 when none.
- [x] 1.5 Run `plugins/issue-driven-dev/scripts/tests/idd-worktree/test.sh` and confirm every fixture passes — GREEN.

## 2. Reference doc + skill integrations (parallel — distinct files)

- [x] 2.1 [P] Write `plugins/issue-driven-dev/references/worktree-isolation.md` documenting the convention: `.claude/worktrees/idd-<N>/` layout, the `idd-worktree.sh create/cleanup/list` workflow, the multi-window (Case B) usage pattern (`create <N>` → `idd-* #N --cwd <path>`), the N-branches→N-PRs convergence model with an explicit pointer that single-cluster-PR work uses sequential `/idd-all-chain`, the shared-state parallel-safety note (no new locking; issue-scoped naming + #76 run_id hardening), and a clearly labelled "Deferred: Case A (within-window agent teams, merge-back)" boundary section so the future change has a stable contract to build on.
- [x] 2.2 [P] Add the Phase 0.5 worktree-branch acceptance clause to `plugins/issue-driven-dev/skills/idd-implement/SKILL.md`: when `--cwd` targets a worktree whose `git branch --show-current` matches `idd/<N>-*` for the issue being implemented, accept it as the feature branch (slug-agnostic), skip branch creation, and bypass the default-branch precondition; non-matching branches follow existing resolution. Verification target: the SKILL prose names the `idd/<N>-*` match and references the worktree-isolation reference.
- [x] 2.3 [P] Add the terminal worktree-GC step to `plugins/issue-driven-dev/skills/idd-close/SKILL.md`: after the issue is closed, invoke `idd-worktree.sh cleanup <N>` as best-effort — absent helper or dirty-refuse surfaces a one-line warning and the close still completes; GC never blocks or fails the close. Add the corresponding `TaskCreate` entry to the idd-close Step 0.5 bootstrap list so it is tracked, per that skill's "TaskCreate list = real step list" rule.
- [x] 2.4 [P] Cross-reference the parallel worktree pattern in `plugins/issue-driven-dev/references/cross-repo-cwd.md` (extend its "See Also" and add a short "Parallel worktree pattern" note) so users discover worktree isolation from the `--cwd` docs.

## 3. Release metadata

- [x] 3.1 [P] Add a CHANGELOG entry for this capability in `plugins/issue-driven-dev/CHANGELOG.md` and bump the version in `plugins/issue-driven-dev/.claude-plugin/plugin.json`, referencing issue #167. (Marketplace sync is a post-merge distribution step handled by `idd-close`, not part of these tasks.)

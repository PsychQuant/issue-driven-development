## Tasks

- [x] Implement the **North-star tracker roadmap-checklist format** requirement by writing plugins/issue-driven-dev/references/north-star-tracker.md. The SOP MUST specify the roadmap-checklist format: under a `## Roadmap` heading, an unfiled stage is an unchecked bullet with no issue reference, and a filed stage is a checked bullet ending in `→ #N`; the presence/absence of the `#N` reference is the emerged-vs-not signal. Also document the 2x2 multi-issue-structure design-space table placing north-star (ordered + progressive) against milestone-first #83 (flat) and bundle-to-chain #81 (ordered + upfront), and when to use north-star vs bundle-mode. Verification: the doc states the format rule (unfiled = plain bullet, filed = `[x] … → #N`) and names #83 / #81 as the complementary cells.

- [x] Implement the **Progressive file-on-start emergence** requirement in the same plugins/issue-driven-dev/references/north-star-tracker.md SOP: document that a stage is filed as its own issue (full IDD lifecycle) only when work begins — never upfront — and that filing updates the tracker's roadmap bullet to checked + linked via idd-edit; contrast this explicitly with `idd-issue --bundle-mode`, which files all children at epic-creation time. Verification: the doc describes the file-on-start convention and the bundle-mode contrast.

- [x] Modify the phase-extraction step of plugins/issue-driven-dev/skills/idd-list/SKILL.md so a tracker-labeled issue is not shown as `(no phase)`. Add a rule: when an open issue carries a `north-star` or `epic` label, derive phase `tracking` and suppress the `/idd-update` suggestion; when the body has a parseable `## Roadmap` checklist, also show `<filed>/<total> stages` where `<filed>` counts checked items carrying an `#<number>` reference. State that this rule also covers milestone-tracked epics from #83. Verification: the SKILL.md phase section documents the tracker-label rule, the roadmap-progress parse (filed = checked + has #ref), and the #83 coverage note.

- [x] Add a cross-reference in plugins/issue-driven-dev/references/bundle-flags.md pointing to references/north-star-tracker.md as the progressive-emergence alternative for roadmaps whose stages are not all known upfront (contrast: bundle-mode files all children at creation time). Verification: bundle-flags.md links to north-star-tracker.md with a one-line contrast.

- [x] Bump plugins/issue-driven-dev/.claude-plugin/plugin.json and the root .claude-plugin/marketplace.json from 2.81.0 to 2.82.0, and add a CHANGELOG.md entry under a new 2.82.0 heading describing the north-star tracker SOP + shared idd-list tracker-phase display rule. Verification: all three version strings read 2.82.0, JSON stays valid, and the CHANGELOG entry references #179 + the shared #83 fix.

- [x] Post a coordination note to issue-driven-development#83 stating that the shared idd-list tracker-phase display fix (north-star + epic labels → `tracking`, no `(no phase)` noise) ships with #179's north-star-tracker change and also covers #83's milestone-tracked epics, so #83 need not re-implement it. Verification: #83 has a comment recording the shared-fix coordination.

{
  "$schema": "https://anthropic.com/claude-code/marketplace.schema.json",
  "name": "issue-driven-development",
  "description": "IDD methodology marketplace: issue-driven workflow for AI-assisted development. Every change starts with an issue, every issue is diagnosed before implementation, every implementation is scope-controlled, every completion is independently verified, every closure is documented.",
  "owner": {
    "name": "Che Cheng",
    "email": "che@example.com"
  },
  "homepage": "https://github.com/PsychQuant/issue-driven-development",
  "allowCrossMarketplaceDependenciesOn": [
    "claude-plugins-official",
    "parallel-ai-agents",
    "codex-pro"
  ],
  "plugins": [
    {
      "name": "issue-driven-dev",
      "version": "3.1.0",
      "description": "v3.1.0: the actionability gate, rounds 2 and 3. Round 1 read `### Complexity` against a closed value domain that the repository's own 159 diagnoses refuted (66 real values wrongly refused) and shipped a three-signal gate no consumer called. Now the tier is the leading whole word (rationale, decoration and ` via` suffixes are legal), the whole line is scanned for a small deferral vocabulary (exit 5, reason `complexity-deferral-marker` — a parked state, not a defect), `### Blocking` is read by a shared fence-aware helper, and idd-list / idd-all / idd-implement / idd-plan actually call the verdict with all three signals in a paginated, `set -e`-safe shape pinned by a drift test. A frozen 159-issue corpus fixture makes 'zero migration' falsifiable: 149 routable, 9 deferral, 1 missing, 0 unparseable. idd-list gains Parked and Needs-diagnosis groups; the #84 blocked surface is preserved verbatim. Round 3 read `### Blocking` per bullet with a leading-token placeholder rule frozen against the repo's 55 real sections (round 2 had withheld 31 of the 47 empty ones, including #316 itself), moved idd-implement's gate ahead of any branch or comment, and retired `blocker:*`.",
      "author": {
        "name": "Che Cheng"
      },
      "source": "./plugins/issue-driven-dev",
      "category": "development"
    },
    {
      "name": "idd-route",
      "version": "0.2.1",
      "description": "v0.2.1: generation-neutral candidate naming (#251) — codex-xhigh; old stats records preserved as history. v0.2.0: Pin to idd-route-swift v0.3.0 — adds update-outcome (idd-close calls this to finalize merged/abandoned) and backfill (one-shot seed from past GitHub verify-comment history; uses gh CLI). The /idd-route:backfill skill now actually works (was placeholder in v0.1.0). idd-close Step 4.5 in issue-driven-dev v2.38.0+ now succeeds instead of gracefully no-op'ing on command-not-found. v0.1.0: Data-driven agent routing for IDD methodology. Recommends Codex (GPT-5.5 xhigh) vs Claude (Opus 4.7 / Sonnet 4.6 / Haiku 4.5) per IDD issue based on observed track record per (complexity, scope_class) bucket with exponential decay (recent decisions weight more, half-life 30 days). Falls back to static heuristic rubric when fewer than 5 data points exist for a candidate (cold start). Wraps the PsychQuant/idd-route-swift binary (universal arm64+x86_64), auto-downloaded via bin/idd-route-wrapper.sh on first use following the version-aware pattern from che-word-mcp. Three thin user-facing skills: /idd-route:recommend (ad-hoc lookup; the issue-driven-dev plugin's idd-diagnose calls the binary directly), /idd-route:stats (human-readable markdown summary by bucket), /idd-route:backfill (one-shot seed from past GH verify-comment history). Per-repo stats at <repo>/.claude/.idd/routing-stats.jsonl, opt-in global mirror at ~/.cache/idd-route/stats.jsonl for cross-repo learning. Opt-out via {enabled: false} in <repo>/.claude/idd-route.json. References controlled signal vocabulary (explicit_acceptance / single_handler / sibling_sweep_needed / etc) shipped in references/signal-vocabulary.md. Static heuristic rubric in references/static-heuristic.md.",
      "author": {
        "name": "Che Cheng"
      },
      "source": "./plugins/idd-route",
      "category": "development"
    }
  ]
}

{
  "$schema": "https://anthropic.com/claude-code/marketplace.schema.json",
  "name": "issue-driven-development",
  "description": "IDD methodology marketplace: issue-driven workflow for AI-assisted development. Every change starts with an issue, every issue is diagnosed before implementation, every implementation is scope-controlled, every completion is independently verified, every closure is documented.",
  "owner": {
    "name": "Che Cheng",
    "email": "che@example.com"
  },
  "homepage": "https://github.com/PsychQuant/issue-driven-development",
  "plugins": [
    {
      "name": "issue-driven-dev",
      "version": "2.37.0",
      "description": "v2.37.0: External-agent / PR mode for idd-verify + use-case routing reference. NEW idd-verify --pr <N> input mode for verifying PRs opened by external agents (Codex via codex exec, Copilot Workspace, remote claw on PsychQuantClaw) — gh pr diff + gh pr checkout so reviewer agents see file context, auto-restore original branch after verify. Plus --commits N / --since <ref> / --branch <name> flags for other input sources. Auto-detect mode (no flag): counts unpushed Refs #N commits since origin/<default> first; if 0, queries open PRs ref'ing #N and AskUserQuestion to pick between local diff vs PR. Issue↔PR correspondence is a hard iron rule: PR mode aborts before invoking 6-AI ensemble if PR body has zero Refs #N or if user-passed issue isn't in PR's Refs set. PR mode flips master comment location: full verify report posts to PR, each ref'd issue gets a 1-line pointer comment back. Capture-master-URL-then-write-pointer SOP enforced. NEW references/external-agent-delegation.md is canonical contract; NEW references/usecase-routing.md closes discoverability gap with 24-row table mapping common scenarios → exact skill chain + flags + contract doc. Backward compat: single-issue invocation idd-verify #42 without flags still works as v2.36 in common case; cluster-PR mode #34 #36 #38 unchanged.",
      "author": {
        "name": "Che Cheng"
      },
      "source": "./plugins/issue-driven-dev",
      "category": "development"
    },
    {
      "name": "idd-route",
      "version": "0.1.0",
      "description": "v0.1.0: Data-driven agent routing for IDD methodology. Recommends Codex (GPT-5.5 xhigh) vs Claude (Opus 4.7 / Sonnet 4.6 / Haiku 4.5) per IDD issue based on observed track record per (complexity, scope_class) bucket with exponential decay (recent decisions weight more, half-life 30 days). Falls back to static heuristic rubric when fewer than 5 data points exist for a candidate (cold start). Wraps the PsychQuant/idd-route-swift binary (universal arm64+x86_64), auto-downloaded via bin/idd-route-wrapper.sh on first use following the version-aware pattern from che-word-mcp. Three thin user-facing skills: /idd-route:recommend (ad-hoc lookup; the issue-driven-dev plugin's idd-diagnose calls the binary directly), /idd-route:stats (human-readable markdown summary by bucket), /idd-route:backfill (one-shot seed from past GH verify-comment history). Per-repo stats at <repo>/.claude/.idd/routing-stats.jsonl, opt-in global mirror at ~/.cache/idd-route/stats.jsonl for cross-repo learning. Opt-out via {enabled: false} in <repo>/.claude/idd-route.json. References controlled signal vocabulary (explicit_acceptance / single_handler / sibling_sweep_needed / etc) shipped in references/signal-vocabulary.md. Static heuristic rubric in references/static-heuristic.md.",
      "author": {
        "name": "Che Cheng"
      },
      "source": "./plugins/idd-route",
      "category": "development"
    }
  ]
}